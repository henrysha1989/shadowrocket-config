# 配置说明

这份文档解释 [`shadowrocket-白名单.conf`](./shadowrocket-白名单.conf) **为什么这么写**。配置文件本身已清空全部注释、只留纯规则 —— 说明都收在这份文档里。

订阅地址、导入步骤、"怎么更新规则集"见 [README](./README.md)。

---

## 📌 版本与形态

* **基线**：手机 Shadowrocket 于 **2026-09-25 16:37** 导出的在用配置。
* **唯一内容改动**：20 条 blackmatrix7 规则集由 `cdn.jsdelivr.net` 改为自建加速站 `git.521989.xyz` 前缀。
  原因：jsDelivr 对 `@master` 分支的缓存最长约 12 小时；自建加速站上游是 raw 的 Fastly 缓存 `max-age=300`（5 分钟）。
* **形态**：无注释、无 BOM、LF 换行、行尾无多余空格；4 个段、69 条规则。

---

## 🧱 结构：四块，从具体到宽泛

顺序是这份配置里**最重要**的部分 —— Shadowrocket/Surge 是 first-match，越具体的规则必须越靠前。

| 块 | 内容 | 为什么在这个位置 |
| :--- | :--- | :--- |
| ① 拦截 | 自用拦截表 + `BlockHttpDNS` / `AdvertisingLite` / `Privacy` | 最先，被拒的请求不再进入后续任何匹配 |
| ② 精确直连 | 自有服务、私网设备、iOS 系统底层（NTP/APNs/OTA/OCSP/联网探测） | 必须早于 ③，否则会被 ③ 的服务列表抢走 |
| ③ 代理 | 自用代理表 + Gemini/Telegram/YouTube/Google/Facebook/Twitter/GitHub/GlobalMedia | 必须早于 ④，否则会被 ④ 的大表泛域名误截 |
| ④ 直连大表 + 微信 + 兜底 | 自用直连表、Apple/China/ChinaMedia/Download、微信 QQ 信令、`GEOIP,CN`、`FINAL` | 兜底，放最后 |

---

## 🔍 逐段要点

### ① 拦截

自用拦截表（`reject-custom.list`，由本机 ADH 管线维护）+ blackmatrix7 的 `BlockHttpDNS`、`AdvertisingLite`（域名集 + 规则集）、`Privacy`（域名集 + 规则集）。

### ② 精确直连

* **自有服务与工具链**：`cdn.jsdelivr.net`、`521989.xyz`、`tailscale.com` / `tailscale.io`（组网直连，不走代理）。
* **私有网段与局域网设备**：`100.64.0.0/10`（运营商级 NAT / Tailscale 网段）、`Lan.list`、`Tesla.list`、`STUN.list`。
* **iOS 系统底层**：NTP（`DST-PORT,123`、`pool.ntp.org`、`time.*`）、APNs（`DST-PORT,5223`、`push.apple.com`、`push-apple.com.akadns.net`）、Apple 网段 `17.0.0.0/8`、OTA（`mesu.apple.com`、`appldnld.apple.com`、`updates*.cdn-apple.com`）、OCSP/CRL（`ocsp.apple.com`、`crl.apple.com`、`gg.apple.com`、`gs.apple.com`、`gdmf.apple.com`）、证书链 CA（DigiCert / Entrust / Let's Encrypt / IdenTrust / Sectigo）、联网探测（`connectivitycheck.gstatic.com`、`detectportal.firefox.com`）。

> 这些域名走直连是为了避免系统更新包、证书校验消耗代理流量，也避免苹果的连通性探测被代理绕一圈。

### ③ 代理

自用代理表（`proxy-custom.list`）+ 一组需要走代理的服务列表。

> **AppleProxy 已于 2026-09-25 移除**（国行设备本来用不了 Apple TV+ / News，本人也不用）。要恢复：把下面这行插回本段末尾即可 ——
> `RULE-SET,https://git.521989.xyz/https://raw.githubusercontent.com/blackmatrix7/ios_rule_script/master/rule/Shadowrocket/AppleProxy/AppleProxy.list,PROXY`
> 移除后果：`tv.apple.com` / `apps.apple.com` / `itunes.apple.com` 等约 10 条域名会解析到国内 IP 而改走直连（对国行是更优路径，也不吃代理流量）；18 条非国内域名仍由 `FINAL,PROXY` 兜住，行为不变；5 条 `USER-AGENT` 规则（`AppleNews*` / `AppleTV*` / `com.apple.news*` / `com.apple.tv*` / `com.apple.trustd*`）一并失效。代价：那 18 条今后靠 DNS 解析结果决定走向，不再是"钉死代理"。

### ④ 直连大表 + 微信 + 兜底

自用直连表（`direct-custom.list`）+ `Apple_Domain` / `China_Domain` / `ChinaMedia` / `Download`，之后是微信 QQ 的信令与腾讯网段，最后 `GEOIP,CN,DIRECT` → `FINAL,PROXY`。

> ⚠️ **顺序约束（改动时勿破坏）**：这些腾讯 IP 段必须留在 ③ 之后。`172.96.0.0/11` 会扫到 Google 的 `172.110.32.0/21`，若提到 ③ 前面，Google / YouTube 的 IP 会被强制直连。
>
> 微信相关：`DST-PORT,3478`（STUN）、`AND,((PROTOCOL,UDP),(DST-PORT,8500-8800))`、`AND,((PROTOCOL,UDP),(DST-PORT,12070))`、`IP-CIDR,172.32.0.0/11`、`172.72/13`、`172.80/12`、`172.96/11`、`172.128/9`、`204.141.0.0/16`、`30.0.0.0/8`、`100.128.0.0/9`、`101.226.0.0/15`（原 `101.226/16` + `101.227/16` 合并）、`180.111.0.0/16`、`183.131.0.0/16`。

---

## ⚙️ `[General]` 关键参数

| 参数 | 值 | 说明 |
| :--- | :--- | :--- |
| `block-quic` | `all-proxy` | 只对**走代理**的连接封 QUIC（443/UDP），直连流量不受影响。比手写 `AND` 规则省开销，且少一次 TCP 回退 |
| `udp-policy-not-supported-behaviour` | `REJECT` | UDP 兜底策略，替代"全端口 UDP 丢弃"的手写规则 |
| `dns-server` | `https://doh.521989.xyz:8443/dns-query/iphone17pm#no-h3` | 自建 DoH；`#no-h3` 关闭自动升级 HTTP/3，省一次协商 |
| `hijack-dns` | `*:53` | 劫持 53 端口 DNS，强制走上面的 DoH |
| `ipv6` / `prefer-ipv6` | `false` | 关闭 IPv6，减少双栈解析与回退 |
| `dns-reject-matching-request-type` | `AAAA` | 直接拒绝 AAAA 查询 |
| `remote-dns` | `true` | 由远端（代理侧）解析 |
| `skip-proxy` / `tun-excluded-routes` | 见文件 | 局域网与特殊域名直连、系统路由排除网段 |

---

## 🌐 `[URL Rewrite]` 与 `[MITM]`

两条 Google 防跳转规则：`google.cn` / `g.cn` → `https://www.google.com`（`$1` 保留路径与查询参数）。写法沿用手机导出的**带引号**形式。

**当前不开 MITM**（`[MITM] enable = false`，`hostname` 已预置），所以只有**明文 HTTP** 那一跳会被重写：

| 入口 | 是否生效 | 原因 |
| :--- | :--- | :--- |
| 敲 `google.cn` | ✅ | 服务端 302 到 `http://www.google.cn/`，那一跳是明文 |
| 敲 `www.google.cn` | ❌ | 全程 HTTPS，返回 200 落地页，没有明文跳转 |
| `google.cn/search?q=x` | ❌ | HTTPS 直接 404，无跳转 |

要让后两者生效，只能开启 MITM 并安装、信任 CA 证书（`hostname` 已写好 `google.cn, *.google.cn, g.cn, *.g.cn`）。

---

## 📝 变更记录

| 日期 | 变更 |
| :--- | :--- |
| 2026-09-25 | 以手机导出配置为基线存档 |
| 2026-09-25 | 结构由 8 段精简为 4 块；规则 71 → 69 条（合并 `101.226/16` + `101.227/16` → `101.226/15`）；修正 5 条被代理列表抢走的死规则（Apple OTA / OCSP / DigiCert / `connectivitycheck.gstatic.com`） |
| 2026-09-25 | 移除 AppleProxy 列表；`[MITM]` 明确 `enable = false` |
| 2026-09-25 | 20 条 blackmatrix7 规则集改走自建加速站（原 jsDelivr `@master`，分支缓存最长约 12 小时） |
| 2026-09-25 | 原 `[Rule]` 段内注释全部移出到本文件，配置文件转为纯规则 |
