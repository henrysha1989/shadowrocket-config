# 配置说明

这份文档解释仓库里两份配置 **为什么这么写**。配置文件本身已清空全部注释、只留纯规则 —— 说明都收在这份文档里。

订阅地址、导入步骤、"怎么更新规则集"见 [README](./README.md)。

---

## 📌 版本与形态

本仓库有**三份**配置，规则逻辑同源。前两份只差「与作者个人基建绑定的那一小部分」；
第三份是**测试通道** = 自用版 + 当前正在验证的改动：

| 文件 | 面向 | 一句话 |
| :--- | :--- | :--- |
| [`shadowrocket-白名单.conf`](./shadowrocket-白名单.conf) | 作者自用（默认） | 含自建 DoH、自建加速站、3 条自用清单 |
| [`shadowrocket-白名单.通用版.conf`](./shadowrocket-白名单.通用版.conf) | **任何人** | 换公共 DoH + 公共 CDN jsDelivr，去掉作者私有项 |
| [`shadowrocket-白名单.测试版.conf`](./shadowrocket-白名单.测试版.conf) | 作者自用（测试通道） | 自用版 + 正在验证的改动，带 5 行说明头 |

下面的「结构」「逐段要点」三份通用；自用版 vs 通用版的差异见 [🆚 自用版 vs 通用版](#-自用版-vs-通用版)；
测试通道当前在测什么，见下面的 **🧪 测试版 T1**。

* **基线**：手机 Shadowrocket 于 **2026-09-25 16:37** 导出的在用配置。
* **自用版的内容改动**：20 条 blackmatrix7 规则集由 `cdn.jsdelivr.net` 改为自建加速站 `git.521989.xyz` 前缀。
  原因：jsDelivr 对 `@master` 分支的缓存最长约 12 小时；自建加速站上游是 raw 的 Fastly 缓存 `max-age=300`（5 分钟）。
* **形态**：无注释、无 BOM、LF 换行、行尾无多余空格；4 个段。
  自用版 **59 条规则 / 24 条规则集**；通用版 **55 条规则 / 21 条规则集**；
  测试版 **59 条规则 / 24 条规则集**（= 自用版 + 2 行 `[General]` 改动，且**带 5 行说明头**）。

---

## 🆚 自用版 vs 通用版

规则的**结构、顺序、绝大多数条目两份完全一致**，差异只有下面 5 处（`diff` 全量核对过）：

| # | 自用版 `shadowrocket-白名单.conf` | 通用版 `shadowrocket-白名单.通用版.conf` | 为什么改 |
| :--- | :--- | :--- | :--- |
| 1 | `dns-server = https://doh.521989.xyz:8443/dns-query/iphone17pm#no-h3` | `dns-server = https://dns.alidns.com/dns-query` | 自建 DoH 是**别人用会直接断网**的项；换成公共 AliDNS DoH |
| 2 | 24 条规则集前缀 `https://git.521989.xyz/https://raw.githubusercontent.com/` | 21 条规则集（blackmatrix7）走公共 CDN `https://cdn.jsdelivr.net/gh/blackmatrix7/ios_rule_script@master/` | 去掉对作者自建加速站的依赖。**选 jsDelivr 而非官方 raw**：2026-09-25 逐条 GET（当时 20 条）jsDelivr **18/20** 可达、官方 raw 只有 **2/20**；后加的 `Apple.list` 亦复验 200 ✅ —— 面向公众用公共 CDN 更稳 |
| 3 | 拦截段首条 `reject-custom.list`（作者自用拦截表） | 无 | 清单内容是作者的，别人加载了也拦不到自己的目标 |
| 4 | 代理段 `proxy-custom.list` / 直连段 `direct-custom.list`（作者自用表） | 无 | 同上 |
| 5 | `DOMAIN-SUFFIX,521989.xyz,DIRECT` | 无 | 作者自有域名，对别人是死规则 |

> [!NOTE]
> 换用通用版后，广告拦截由 blackmatrix7 的 `BlockHttpDNS` / `AdvertisingLite`（域名集 + 规则集）/ `Privacy`（域名集 + 规则集）承担，覆盖面仍是主体；只是少了作者私人的那份补充清单。
>
> 通用版 21 条规则集全部走公共 CDN jsDelivr，**不依赖作者任何服务**。代价是 jsDelivr 对 `@master` 分支的缓存最长约 12 小时（自用版走加速站是 5 分钟级）—— 想要更快的新鲜度，把前缀换回 `https://raw.githubusercontent.com/blackmatrix7/ios_rule_script/master/` 或自备加速站即可。

---

## 🧪 测试版 T1：DNS 两项

`shadowrocket-白名单.测试版.conf` = **自用版 + 下面两行改动**（`diff` 全量核对：只有这 2 处语义差异 + 5 行说明头）。
测试通道的规矩：**文件路径、订阅链接、二维码固定不变**，以后每个实验直接覆盖这一个文件，手机不用重新加配置。

| 参数 | 自用版 | 测试版 | 手册依据 |
| :--- | :--- | :--- | :--- |
| `dns-fallback-system` | `false` | **`true`** | 手册**未收录**此键（命名上应是「回退时用系统 DNS」）。有文档的等价写法是 `fallback-dns-server = system`（手册：「`system` 表示回退到系统 DNS，清空备用 DNS 等于将其设置为 `system`」） |
| `dns-direct-system` | `false` | **`true`** | 隐式参数（无 UI）：「**直连的域名类规则**使用系统 DNS 进行查询」 |

**为什么测这两个**

1. **`dns-direct-system`** —— 现状下直连域名也全部打给自建 DoH（HTTPS + 远程往返 + TLS）。
   打开后这批解析走 iOS 系统解析器：**本地 UDP、跨 App 共享缓存、无 TLS**。
   注意它的**生效范围只有「直连的域名类规则」**：手册「直连DNS `direct-dns-server`」条目写明
   「其他 **IP 规则**触发的直连类解析遵循 DNS 覆写的设定」—— 所以 `GEOIP,CN` 兜底那批**仍然走自建 DoH**。
2. **`dns-fallback-system`** —— 现状备用 DNS 是**两个 DoT**（`tls://223.5.5.5, tls://1.12.12.12`）：
   一旦自建 DoH 失败或超过 2s，**每个查询**都要多走 1–2 次 TLS 握手。
   改成系统 DNS 后，回退路径变成一条本地 UDP 查询。
   ⚠️ 它**平时一次都不生效**（只在回退时走），所以这一项是「治最坏情况的保险」，不是日常提速。

**代价**：直连域名的解析对**运营商/路由器可见**（原本由自建 DoH 隐藏）。
自用直连表里基本都是国内域名，污染风险很低；对 DNS 隐私有要求就别开 `dns-direct-system`。

**判读方法（重要）**：`dns-direct-system` 的效果**不在代理日志里**，要看单独的 **DNS 日志**。

| 指标 | 怎么取 |
| :--- | :--- |
| DNS 查询量/小时 | 数据 → 代理 → DNS → 启用日志记录，跑一天后导出，与开测试版的前一天对比 |
| 回退触发率 | 手册：**DNS 日志里响应时间 > 2s 即表示正在触发回退机制** —— 这项决定 `dns-fallback-system` 值不值得留 |
| 连通性回归 | 打开 `dns-direct-system` 后若出现某些国内站点解析异常（被 ISP 劫持），改回 `false` 即可 |

**回退**：重新导入自用版；或把这两行改回 `false`。

---

## 🧱 结构：四块，从具体到宽泛

顺序是这份配置里**最重要**的部分 —— Shadowrocket/Surge 是 first-match，越具体的规则必须越靠前。

| 块 | 内容 | 为什么在这个位置 |
| :--- | :--- | :--- |
| ① 拦截 | 自用拦截表 + `BlockHttpDNS` / `AdvertisingLite` / `Privacy` | 最先，被拒的请求不再进入后续任何匹配 |
| ② 精确直连 | 自有服务、私网设备、iOS 系统底层（NTP/APNs/OTA/OCSP/联网探测） | 必须早于 ③，否则会被 ③ 的服务列表抢走 |
| ③ 代理 | 自用代理表 + Gemini/Telegram/YouTube/Google/Facebook/Twitter/GitHub/GlobalMedia | 必须早于 ④，否则会被 ④ 的大表泛域名误截 |
| ④ 直连大表 + 信令端口 + 兜底 | 自用直连表、Apple（`Apple_Domain` + `Apple` 两半）/China/ChinaMedia/Download、微信 QQ 打洞端口、`GEOIP,CN`、`FINAL` | 兜底，放最后 |

---

## 🔍 逐段要点

### ① 拦截

自用拦截表（`reject-custom.list`，由本机 ADH 管线维护；**通用版没有这条**）+ blackmatrix7 的 `BlockHttpDNS`、`AdvertisingLite`（域名集 + 规则集）、`Privacy`（域名集 + 规则集）。

### ② 精确直连

* **自有服务与工具链**：`cdn.jsdelivr.net`、`521989.xyz`（**仅自用版**）、`tailscale.com` / `tailscale.io`（组网直连，不走代理）。
* **私有网段与局域网设备**：`100.64.0.0/10`（运营商级 NAT / Tailscale 网段）、`Lan.list`、`Tesla.list`、`STUN.list`。
* **iOS 系统底层**：NTP（`DST-PORT,123`、`pool.ntp.org`、`time.*`）、APNs（`DST-PORT,5223`、`push.apple.com`）、Apple 网段 `17.0.0.0/8`、OTA（`mesu.apple.com`、`appldnld.apple.com`、`updates*.cdn-apple.com`）、OCSP/CRL（`ocsp.apple.com`、`crl.apple.com`、`gg.apple.com`、`gs.apple.com`、`gdmf.apple.com`）、**第三方 CA 的证书吊销查询**（DigiCert / Entrust / Let's Encrypt 的 `lencr.org` / IdenTrust / Sectigo）。
  > 证书吊销查询发生在 TLS 握手阶段：走代理会多一次往返、吃代理流量，代理异常还可能让校验软失败，故标直连。
  > 🧹 2026-09-25 调整：`letsencrypt.org` → **`lencr.org`**（LE 已停用 OCSP，CRL 迁至 `x1/x2.c.lencr.org`；旧域名实测 `ENOTFOUND`，等于没挡）；删除 `detectportal.firefox.com`（Firefox 专用，作者不用）与 `connectivitycheck.gstatic.com`（Android/Chrome 探测，作者不用 Chrome）。
  > 原先还有一条 `DOMAIN-SUFFIX,push-apple.com.akadns.net`，已随本次清理删除 —— APNs 的 SNI 是 `*.push.apple.com`，`.akadns.net` 只是它的 CNAME 目标，不会成为请求 Host；即便成为，第 ④ 块的 `Apple_Domain.list`（含 `.akadns.net`）也会兜住。

> 这些域名走直连是为了避免系统更新包、证书校验消耗代理流量，也避免苹果的连通性探测被代理绕一圈。

### ③ 代理

自用代理表（`proxy-custom.list`；**通用版没有这条**）+ 一组需要走代理的服务列表。

> **AppleProxy 已于 2026-09-25 移除**（国行设备本来用不了 Apple TV+ / News，本人也不用）。要恢复：把下面这行插回本段末尾即可 ——
> `RULE-SET,https://git.521989.xyz/https://raw.githubusercontent.com/blackmatrix7/ios_rule_script/master/rule/Shadowrocket/AppleProxy/AppleProxy.list,PROXY`
> 移除后果：`tv.apple.com` / `apps.apple.com` / `itunes.apple.com` 等约 10 条域名会解析到国内 IP 而改走直连（对国行是更优路径，也不吃代理流量）；18 条非国内域名仍由 `FINAL,PROXY` 兜住，行为不变；5 条 `USER-AGENT` 规则（`AppleNews*` / `AppleTV*` / `com.apple.news*` / `com.apple.tv*` / `com.apple.trustd*`）一并失效。代价：那 18 条今后靠 DNS 解析结果决定走向，不再是"钉死代理"。

### ④ 直连大表 + 信令端口 + 兜底

自用直连表（`direct-custom.list`；**通用版没有这条**）+ **Apple 规则集的两半**（`Apple_Domain` + `Apple`）+ `China_Domain` / `ChinaMedia` / `Download`，之后是微信 QQ 的打洞端口，最后 `GEOIP,CN,DIRECT` → `FINAL,PROXY`。

> **Apple 为什么要两个文件**：blackmatrix7 把每个规则集拆成两半 —— `X_Domain.list` 是**纯域名**（给 `DOMAIN-SET` 用，1560 条），`X.list` 是**非域名规则**（`DOMAIN-KEYWORD` / `USER-AGENT` / `IP-CIDR`）。
> `Apple.list` 里**一条 `DOMAIN` 都没有**（2026-09-25 实测：43 条内容行 = KEYWORD 7 + USER-AGENT 23 + IP-CIDR 13），所以它**不能替代** ② 段那些显式域名，只补 IP/UA/关键词这三条判据。
> 本配置对 `AdvertisingLite` / `Privacy` 一直是两半都用；Apple 原先只有域名那一半，**2026-09-25 补齐**。
> ⚠️ 两点注意：① `Apple.list` 排在 ④，因此 ③ 段 `GlobalMedia` 的 `USER-AGENT,AppleTV*` / `com.apple.tv*` 仍会让 Apple TV 走代理（若要让 Apple 全直连，需把这一行挪到 ② 段）；② 它含 `USER-AGENT,Music*` / `Maps*` / `Wallet*`、`DOMAIN-KEYWORD,testflight` 这类**宽泛模式**，有误伤非 Apple 应用的可能，而 `USER-AGENT` 是本配置里少数真正线性匹配的类型。

> **微信 / QQ 打洞**：只保留**端口维度**的三条 —— `DST-PORT,3478`（STUN）、`AND,((PROTOCOL,UDP),(DST-PORT,8500-8800))`、`AND,((PROTOCOL,UDP),(DST-PORT,12070))`。它们对**任意对端 IP** 生效，不依赖对端在哪个网段。
>
> 之后是三条国内快路径：`IP-CIDR,101.226.0.0/15`（原 `101.226/16` + `101.227/16` 合并）、`180.111.0.0/16`、`183.131.0.0/16`（均为中国电信），纯粹为省一次 `GEOIP,CN` 解析。
>
> 🧹 **2026-09-25 清理**：原先还有一批被标为「腾讯内网 / 信令」的 IP 段 —— `172.32.0.0/11`、`172.72.0.0/13`、`172.80.0.0/12`、`172.96.0.0/11`、`172.128.0.0/9`、`204.141.0.0/16`、`30.0.0.0/8`、`100.128.0.0/9`。经逐一查证，它们**全部是境外真实公网段**（T-Mobile USA、Microsoft、Akamai、NTT America、美国国防部等），并非腾讯网段，已整批移除。详见「🔒 个人定制项」E 组。
>
> 注：原先那条「`172.96.0.0/11` 会扫到 Google `172.110.32.0/21`，必须留在 ③ 之后」的顺序约束，随该段移除而**自动失效**。

---

## 🔒 自用版里的「个人定制项」（别人用了没效果）

> 这一节只针对**自用版**。通用版已把这些全部摘掉或替换。列出来是为了说明「哪些东西是绑在作者身上的」，
> 而不是配置本身的规则设计。

### A. 硬依赖 —— 别人用了会直接坏

| 项 | 位置 | 影响 |
| :--- | :--- | :--- |
| 自建 DoH `https://doh.521989.xyz:8443/dns-query/iphone17pm#no-h3` | `[General] dns-server` | **唯一一条会让别人直接断网的**：别人的设备解析不了这个域名 / 证书不认，DNS 整条失效。通用版已换成 `https://dns.alidns.com/dns-query` |

### B. 依赖作者的基建或数据

| 项 | 位置 | 别人拿到会怎样 |
| :--- | :--- | :--- |
| 自建 GitHub 加速站前缀 `git.521989.xyz` | 全部 24 条规则集 URL | 能加载，但整条规则集链路挂在作者的服务上；通用版改走公共 CDN jsDelivr（raw 官方链路实测可达性差，见「🆚」一节） |
| 3 条自用清单 `henrysha1989/shadowrocket-adr-rules`（`reject-custom` / `proxy-custom` / `direct-custom`） | ①③④ 段 | 能加载，但清单内容（拦截/代理/直连目标）是作者自己的，别人加载了也拦不到自己要的 |
| `DOMAIN-SUFFIX,521989.xyz,DIRECT` | ② 段 | 作者自有域名，对别人是死规则 |

### C. 按作者所在网络 / 运营商 / 习惯调的（无害，但对别人无意义）

| 项 | 位置 | 说明 |
| :--- | :--- | :--- |
| 微信 / QQ 打洞**端口**：`DST-PORT 3478`、`AND UDP 8500-8800`、`AND UDP 12070` | ④ 段 | 按「国内通讯信令 / STUN 打洞直连」整理，但只保留端口维度 —— 对任意对端 IP 生效，任何网络环境都成立 |
| 国内快路径 `101.226.0.0/15`（上海电信）、`180.111.0.0/16`（江苏电信）、`183.131.0.0/16`（浙江电信） | ④ 段 | 与 `GEOIP,CN` 功能重复，只是省一次解析；**通用版保留**（对大陆用户无害），可自行删减 |
| `DOMAIN,cdn.jsdelivr.net,DIRECT` | ② 段 | 两份都留：自用版是为了从 jsDelivr 拉配置时不绕代理；通用版本身就是走 jsDelivr 拉规则集，更该直连 |
| `google.cn` / `g.cn` → `www.google.com` 302 + `[MITM] hostname` | `[URL Rewrite]` / `[MITM]` | 作者个人习惯的 Google 中国域名跳转 |
| 整组国内 DNS 取舍：`fallback/direct/proxy-dns-server`（阿里 DNS + DNSPod）、`dns-reject-matching-request-type = AAAA`、`hijack-dns = *:53`、`block-quic = all-proxy`、`ipv6 = false` | `[General]` | 全是**中国大陆网络环境**的取舍；境外用户照抄基本是反效果 |
| `GEOIP,CN,DIRECT` → `FINAL,PROXY` | ④ 段 | 「国内直连 / 国外代理」的分流哲学；且需要用户自带代理节点（本仓库不含节点） |

### D. 通用项（两份都有，别人同样适用）

`skip-proxy` / `tun-excluded-routes` 里的私有网段、Apple 推送/OTA/OCSP 段、`17.0.0.0/8`（Apple）、
证书链 CA 段、`100.64.0.0/10`（CGNAT）、`tailscale.com` / `tailscale.io`、
21 条 blackmatrix7 公开规则集（含 Apple 的 `Apple_Domain` + `Apple` 两半）、`GEOIP,CN` + `FINAL`。

> 一句话：**真正「别人用完全没效果」的只有 A 组那一条自建 DoH**；B 组依赖作者基建，C 组是作者的本地环境与偏好。

### E. 已移除（2026-09-25 清理）

原先 ④ 段有一批被注释成「腾讯内网 / 信令」的 `IP-CIDR ... DIRECT`，逐一查证后**全部是境外真实公网段**，已整批删除：

| 段 | 实测归属（RIPEstat / ASN） | 移除理由 |
| :--- | :--- | :--- |
| `100.128.0.0/9` | **T-Mobile USA**（AS21928） | 真实公网，非 CGNAT（RFC6598 只有 `100.64.0.0/10`） |
| `172.32.0.0/11` | **T-Mobile USA**（AS21928） | 同上 |
| `172.72.0.0/13`、`172.80.0.0/12`、`172.96.0.0/11` | 从 `172.64.0.0/10` 挖掉 Cloudflare `172.64.0.0/13` 后的残段（eSited、IPTP 等） | 与 Cloudflare 的取舍逻辑随整块移除而失效 |
| `172.128.0.0/9` | **Microsoft**（`172.128/11` 等，AS8075）+ Akamai/Charter | 误伤面最大的一条 |
| `204.141.0.0/16` | **NTT America**（AS2914） | 真实公网 |
| `30.0.0.0/8` | **美国国防部 DoD**（AS749） | 与微信/QQ 无任何关系，纯属民间清单以讹传讹 |

> 依据：这些段在 blackmatrix7 的 `WeChat.list` / `STUN.list` / `Tencent.list` 里**零命中**，属"观察到的通话对端 IP"拼出来的民间清单。
> 替换方案：打洞交给 `DST-PORT,3478` + 两条 UDP 端口规则（对任意 IP 生效），其余境外流量交给 `GEOIP,CN` → `FINAL,PROXY`。
> 前提：代理节点需支持 UDP 转发；否则应把 `udp-policy-not-supported-behaviour` 设为 `DIRECT`（而非 `REJECT`），避免 UDP 被直接丢弃。

### F. 证书链 / 联网探测的三处调整（2026-09-25）

| 项 | 动作 | 实测依据 |
| :--- | :--- | :--- |
| `DOMAIN-SUFFIX,letsencrypt.org` | → **`DOMAIN-SUFFIX,lencr.org`** | LE 2025 年停用 OCSP，CRL 迁到 `x1.c.lencr.org` / `x2.c.lencr.org`（解析正常）。旧域名 `ocsp.int-x3.letsencrypt.org` / `crl.letsencrypt.org` 双双 **ENOTFOUND** ⇒ 原来那条等于没挡 |
| `DOMAIN,detectportal.firefox.com` | **删除** | Firefox 专用强制门户探测（`/success.txt`）；作者不用 Firefox。iOS 的门户探测是 `captive.apple.com`（在 `skip-proxy` 里），不经过它 |
| `DOMAIN,connectivitycheck.gstatic.com` | **删除** | Android / Chrome 的连通性探测（`/generate_204`）；作者不用 Chrome/Android。删除后会被 ③ 的 `Google.list`（`DOMAIN-SUFFIX,gstatic.com`）接走 → 走代理，无碍 |

保留未动的第三方 CA：`digicert.com`、`digicert.cn`、`entrust.net`、`identrust.com`、`sectigo.com` —— 实测 OCSP/CRL 端点均在解析中（`digicert.cn` 落中国电信 `58.218.208.12`；Entrust / Sectigo 托管在 Cloudflare）。

---

## ⚙️ `[General]` 关键参数

| 参数 | 值 | 说明 |
| :--- | :--- | :--- |
| `block-quic` | `all-proxy` | 只对**走代理**的连接封 QUIC（443/UDP），直连流量不受影响。比手写 `AND` 规则省开销，且少一次 TCP 回退 |
| `udp-policy-not-supported-behaviour` | `REJECT` | UDP 兜底策略，替代"全端口 UDP 丢弃"的手写规则 |
| `dns-server` | 自用版 `https://doh.521989.xyz:8443/dns-query/iphone17pm#no-h3` / 通用版 `https://dns.alidns.com/dns-query` | 自用版走作者自建 DoH（`#no-h3` 关闭自动升级 HTTP/3，省一次协商）；通用版换公共 AliDNS DoH |
| `hijack-dns` | `*:53` | 劫持 53 端口 DNS，强制走上面的 DoH |
| `ipv6` / `prefer-ipv6` | `false` | 关闭 IPv6，减少双栈解析与回退 |
| `dns-reject-matching-request-type` | `AAAA` | 直接拒绝 AAAA 查询 |
| `remote-dns` | `true` | 由远端（代理侧）解析 |
| `dns-direct-system` | `false`（测试版 `true`） | 隐式参数：**直连的域名类规则**用系统 DNS 解析。只影响域名类规则，`GEOIP,CN` 兜底那批仍走 `dns-server`。测试版打开它，理由见「🧪 测试版 T1」 |
| `fallback-dns-server` / `dns-fallback-system` | `tls://223.5.5.5, tls://1.12.12.12` + `false`（测试版改 `true`） | 覆写 DNS 失败或 >2s 时的备用 DNS。`dns-fallback-system` 手册未收录；有文档的等价写法是 `fallback-dns-server = system`。测试版让回退走系统 DNS，省掉回退路径上的 1–2 次 TLS 握手 |
| `skip-proxy` / `tun-excluded-routes` | 见文件 | 局域网与特殊域名直连、系统路由排除网段 |

---

## 🌐 `[URL Rewrite]` 与 `[MITM]`

两条 Google 防跳转规则：`google.cn` / `g.cn` → `https://www.google.com`（`$1` 保留路径与查询参数）。写法沿用手机导出的**带引号**形式。

**当前不开 MITM**（`[MITM] enable = false`，`hostname` 已预置），所以只有**明文 HTTP** 那一跳会被重写：

| 入口 | 是否生效 | 原因 |
| :--- | :--- | :--- |
| 敲 `google.cn` | ✅ | 服务端 302 到 `http://www.google.cn/`，那一跳是明文 |
| 敲 `www.google.cn` | ❌ | 全程 HTTPS，返回 200 落地页，没有明文跳转 |
| `google.cn/search?q=x` | ❌ | HTTPS 直接 404，无跳转 |

要让后两者生效，只能开启 MITM 并安装、信任 CA 证书（`hostname` 已写好 `google.cn, *.google.cn, g.cn, *.g.cn`）。

---

## 📝 变更记录

| 日期 | 变更 |
| :--- | :--- |
| 2026-09-25 | 以手机导出配置为基线存档 |
| 2026-09-25 | 结构由 8 段精简为 4 块；规则 71 → 69 条（合并 `101.226/16` + `101.227/16` → `101.226/15`）；修正 5 条被代理列表抢走的死规则（Apple OTA / OCSP / DigiCert / `connectivitycheck.gstatic.com`） |
| 2026-09-25 | 移除 AppleProxy 列表；`[MITM]` 明确 `enable = false` |
| 2026-09-25 | 20 条 blackmatrix7 规则集改走自建加速站（原 jsDelivr `@master`，分支缓存最长约 12 小时） |
| 2026-09-25 | 原 `[Rule]` 段内注释全部移出到本文件，配置文件转为纯规则 |
| 2026-09-25 | **新增通用版 `shadowrocket-白名单.通用版.conf`**：换公共 DoH（AliDNS）、20 条规则集改走公共 CDN jsDelivr、摘掉 3 条自用清单与 `521989.xyz`；新增「🆚 自用版 vs 通用版」与「🔒 个人定制项」两节 |
| 2026-09-25 | **清理"伪腾讯信令"IP 段**：删除 `100.128/9`、`172.32/11`、`172.72/13`、`172.80/12`、`172.96/11`、`172.128/9`、`204.141/16`、`30/8`（经查证全为境外真实公网段）；同时删 `push-apple.com.akadns.net`（CNAME 别名，冗余）。规则 69 → **60 条**（通用版 65 → 56）；新增「E. 已移除」小节 |
| 2026-09-25 | **补齐 Apple 规则集的另一半**：④ 段新增 `RULE-SET,.../Apple/Apple.list,DIRECT`（与 `Apple_Domain.list` 配对）。规则 60 → **61 条**、规则集 23 → **24**（通用版 56 → 57 / 20 → 21） |
| 2026-09-25 | **证书链 / 探测调整**：`letsencrypt.org` → **`lencr.org`**（LE 停用 OCSP、CRL 已迁域，旧域名实测失效）；删除 `detectportal.firefox.com`（Firefox 专用）与 `connectivitycheck.gstatic.com`（Android/Chrome 专用）。规则 61 → **59 条**（通用版 57 → 55）；新增「F. 证书链 / 联网探测的三处调整」 |
| 2026-09-26 | **新增测试通道 `shadowrocket-白名单.测试版.conf`**（= 自用版 + T1：`dns-direct-system` / `dns-fallback-system` 改 `true`，带 5 行说明头）；新增「🧪 测试版 T1：DNS 两项」小节与两张二维码。**路径、订阅链接、二维码自此固定**，后续实验直接覆盖该文件 |
